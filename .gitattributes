# Normalize line endings to LF for cross-platform consistency.
# Python / Markdown / config files use LF on all platforms.
* text=auto eol=lf

# Binary files (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary
*.gz binary
