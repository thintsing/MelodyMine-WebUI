# MelodyMine — Python Dependencies
# Auto-installed by: python music_helper.py setup
yt-dlp>=2026.6.9
requests>=2.34.2
pysocks>=1.7.1

# FFmpeg (no system install needed — this pip package bundles a static binary)
imageio-ffmpeg>=0.6.0

# Spotify URL support (hard-capped to major 4 — MelodyMine imports spotdl's
# internal SpotifyClient API, which breaks across major versions).
# To upgrade: test with the new version, then bump the upper bound here and
# in melodymine/spotify_helper.py (SPOTDL_PACKAGES) and DEP_COMPAT in melodymine_common.py.
spotdl>=4.5.0,<5.0.0
aioslsk>=1.6.3

# YouTube Music metadata search (optional — music_helper.py falls back to
# yt-dlp or bilibili search when not installed)
ytmusicapi>=1.14.0
